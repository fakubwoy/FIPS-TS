# Factory Inventory and Production System

## Backend

To run the backend, follow these steps:

```bash
cd backend
npm install
npm run dev
```

## Frontend

To run the backend, follow these steps:

```bash
cd frontend
npm install
npm run dev
```